# RAPA GO — npm workspace configuration

# Ensure workspaces are hoisted properly
workspaces-update=true

# Prevent accidental publish of private packages
access=restricted

# Save exact versions to avoid unexpected upgrades
save-exact=true

# Do not fund messages in CI
fund=false

# Audit level: fail only on high or critical
audit-level=high
