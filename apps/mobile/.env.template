# RAPA GO — Mobile Environment Variables (template)
# Solo variables públicas (VITE_ prefix). Sin datos sensibles.

# API
VITE_API_BASE_URL=https://your-api-domain.railway.app/api

# Google Maps Embed API (opcional — sin key usa fallback offline)
VITE_GOOGLE_MAPS_API_KEY=

# Opcional: Sentry para errores
# VITE_SENTRY_DSN=
