# RAPA GO — Mobile Production Environment
# Renombrar a .env.production antes de build:prod
# Solo variables públicas (prefijo VITE_). Sin secretos.

VITE_API_BASE_URL=https://your-api-domain.railway.app/api
NODE_ENV=production
